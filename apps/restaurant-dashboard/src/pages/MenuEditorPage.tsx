export function MenuEditorPage() {
  return (
    <div>
      <h2>Menü editörü</h2>
      <p style={{ color: 'var(--hashtap-muted)' }}>
        POS'tan gelen SKU / fiyat / KDV (iş gerçeği) burada salt-okunur.
        HashTap'in sahiplendiği alanlar (fotoğraf, i18n açıklama, görünürlük, öne çıkarma) düzenlenebilir.
      </p>
    </div>
  );
}
